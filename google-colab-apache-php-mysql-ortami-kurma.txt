# Paketleri güncelle ve kur
!apt-get update
!apt-get install -y apache2 php php-mbstring php-xml mysql-server phpmyadmin wget

# Servisleri başlat
!service apache2 start
!service mysql start

!mysql -u root -p1234 -e "CREATE USER 'pma'@'localhost' IDENTIFIED BY 'pmapass';"
!mysql -u root -p1234 -e "GRANT ALL PRIVILEGES ON phpmyadmin.* TO 'pma'@'localhost'; FLUSH PRIVILEGES;"
!service mysql restart


# phpMyAdmin'i Apache'ye bağla
!ln -s /usr/share/phpmyadmin /var/www/html/phpmyadmin

# Test index.php dosyası
%%writefile /var/www/html/index.php
<?php
echo 'test123';
?>

# Cloudflared indir ve kur
!wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
!dpkg -i cloudflared-linux-amd64.deb

# HTTP için tünel aç (arka planda)
!nohup cloudflared tunnel --url http://localhost:80 --no-autoupdate > cloudflared_http.log 2>&1 &

# SSH için tünel aç (arka planda)
!nohup cloudflared tunnel --url ssh://localhost:22 --no-autoupdate > cloudflared_ssh.log 2>&1 &
